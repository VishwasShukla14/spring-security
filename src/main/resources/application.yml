spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: data
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/security
    username: postgres
    password: root

